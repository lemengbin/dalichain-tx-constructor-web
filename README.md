# dalichain-tx-constructor-web
web of dalichain transaction constructor

## Usage
### 1. Requirements
`apt-get install nodejs-legacy npm -y`

### 2. Pull dependencies
`npm install`

### 3. Start server
`node app.js`

Note: default server port is 1991, you can modify port in app.js
